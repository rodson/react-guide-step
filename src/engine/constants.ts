/** Default gap between the target element and the tooltip (px) */
export const DEFAULT_OFFSET = 10;

/** Arrow size in px */
export const ARROW_SIZE = 8;

/** Default highlight padding around the target element (px) */
export const DEFAULT_HIGHLIGHT_PADDING = 4;

/** Opposite side mapping for flip logic */
export const OPPOSITE_SIDE: Record<string, string> = {
  top: 'bottom',
  bottom: 'top',
  left: 'right',
  right: 'left',
};
